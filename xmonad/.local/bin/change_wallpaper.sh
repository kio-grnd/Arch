#!/bin/bash

# Cambiar el fondo de pantalla de forma continua
while true; do
    # Cambia el fondo de pantalla usando imágenes de la carpeta especificada
    feh --randomize --bg-fill ~/.config/wallpapers/*
done

